# Supabase Runbook

- Check auth settings for Magic Link only.
- Verify redirect URLs include `/auth/callback` for staging + prod.
- Update email templates from `emails/`.
- Confirm RLS policies in `supabase/migrations`.
